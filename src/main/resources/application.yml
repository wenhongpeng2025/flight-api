spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flight_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: 6Tfcvgy&*Uhbnji
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none  # 禁用Hibernate自动生成DDL，避免与schema.sql冲突
  sql:
    init:
      mode: always   # 确保每次启动都执行schema.sql和data.sql

springdoc:
  api-docs:
    path: /v3/api-docs  # 自定义API文档路径
  swagger-ui:
    path: /swagger-ui.html  # 自定义Swagger UI路径

jwt:
  secret: J9q4X8z2R7v1Y6w3K5pL0nM8bV4cN6xQ1  # JWT签名密钥（生产环境应使用更复杂的随机字符串）
  expiration: 3600000          # 过期时间（毫秒），1小时
  refreshExpiration: 604800000                # 7天过期
